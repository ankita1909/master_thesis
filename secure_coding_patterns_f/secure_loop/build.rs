//extern crate cc;

fn main() {
  //  cc::Build::new()
    //    .file("example.c")
      //  .compile("example");
        /* 
        cbindgen::Builder::new()
        .with_crate("ffi_use_rust_in_c")
        .generate()
        .expect("Unable to generate bindings")
        .write_to_file("bindings.h");
        */
}